# 20 — AI Sidebar Shell (UI only)

Goal: Placeholder that later becomes real.

## Implementation
- `components/editor/AISidebar.tsx`: tabs `AI Architect` (scrollable chat, starter chips, bubbles, composer with Shift+Enter multiline) + `Specs` (Generate btn + placeholder card). No backend.
- Replace old `<aside>` in workspace client.

## Verify
- [ ] Tabs render; composer multiline works; build passes.
