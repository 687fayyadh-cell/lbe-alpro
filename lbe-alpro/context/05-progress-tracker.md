# Progress Tracker

> The only file that updates constantly. Agent reads this to resume in one prompt.

## Current phase

`none` — project initialized, boilerplate cleaned (`Ghost AI` centered div, Tailwind only in `globals.css`).

## Current goal

TBD — start at `feature-specs/01-design-system.md`.

## In progress

- (none)

## Completed

- (none)

## Coming next

- 01 design-system → 02 editor-shell → 03 auth → 04 project-dialogues → 05 prisma → 06 project-apis → 07 wire-editor-home → 08 workspace-shell → 09 share-dialogue → 10 liveblocks-setup → 11 base-canvas → 12 shape-panel → … → 29 spec-ui-integration

## Architectural decisions

- (append one line per unit, e.g. `F05: Prisma models Project/ProjectCollaborator, client at lib/prisma.ts via pg adapter`)

## Session notes

- (versions, gotchas: e.g. Next 16 uses `proxy.ts` not `middleware.ts`; Blob `private` + `allowOverwrite:true`; Liveblocks storage under `flow` key)
